# Curated langref doctest subset, run when the full langref build is skipped
# (SKIP_LANGREF=1, ZIG_LANGREF_CRITICAL=1). One example name per line, no
# .zig suffix; blank lines and '#' comments ignored by _langref.sh.
#
# Ported from the 0.16 track (branch mnt/v0.16.0_18-ppc64le-bootstrap), where
# it was curated against 0.16 source and measured under qemu-ppc64le at
# ~2min per executing example, seconds per compile-only one. Budget there:
# 16 executing, 50 compile-only.
#
# VALIDATED against 0.17 doc/langref on board 35049829611 (2026-09-16):
# 64 pass, 0 fail, 0 missing expected after this edit. `builtin.CallModifier
# struct` was REMOVED - it exists in 0.16 doc/langref but not in snapshot
# 2131, a genuine upstream divergence. Names absent from the source tree are
# still reported rather than fatal; re-validate before any zig snapshot bump.
#
# cImport_builtin was REMOVED after the 0.16 track measured it hitting the
# 300s per-example timeout to the centisecond on both ppc64le and riscv64
# (board 35044080243). It drives translate-c/clang, which does not finish
# inside the step budget under emulation. Restore it only together with a
# per-example timeout override, and only once its real cost is measured.
#
# ORDER IS SIGNIFICANT. _langref.sh applies a total wall-clock budget and
# breaks the loop when it is exhausted, so the TAIL of this file is the
# sacrificial region. The nine PT_PHDR probe examples are listed FIRST so a
# truncated run still covers the case this subset exists to protect; the
# remainder stays alphabetical and is what gets dropped under budget pressure.
#
# WARNING: several names contain SPACES. Consumers must read this file
# line-by-line and quote the name; do not word-split.
# --- PT_PHDR / dl_iterate_phdr probe set (highest value; keep first) ---
result_location_interfering_with_swap
testing_failure
testing_detect_leak
runtime_division_by_zero
runtime_unwrap_null
test_comptime_call_extern_function
test_struct_result
stack_trace
error_return_trace

# --- remainder, alphabetical ---
Assembly Syntax Explained
TopLevelFields
addWithOverflow_builtin
anonymous_struct_name
build
build_c
catch
catch_err_return
checking_null_in_zig
cli_allocation
compile-time_duck_typing
compile_variables
constant_identifier_cannot_change
destructuring_vectors
enum_export
enum_export_error
export_any_symbol_name
export_builtin
fibonacci_comptime_infinite_recursion
float_mode_obj
float_special_values
generic_data_structure
handle_error_with_catch_block
libc_export_entry_point
not_atomic_cmpxchgStrong
packed_struct_mmio
runtime_overflow
runtime_reaching_unreachable
runtime_vs_comptime
sentinel-terminated_pointer
single_value_error_set
single_value_error_set_shortcut
struct_default_value
test_basic_slices
test_compileLog_builtin
test_comptime_division_by_zero
test_comptime_index_out_of_bounds
test_comptime_invalid_cast
test_comptime_invalid_enum_cast
test_comptime_invalid_error_code
test_comptime_overflow
test_comptime_unwrap_null
test_comptime_wrong_union_field_access
test_illegal_behavior
test_incorrect_pointer_alignment
test_intCast_builtin
test_misaligned_pointer
test_missized_packed_struct
test_sentinel_mismatch
test_shadowing
test_string_literal_to_slice
test_thread_local_variables
test_unhandled_enumeration_value
test_without_setEvalBranchQuota_builtin
var_must_be_initialized
