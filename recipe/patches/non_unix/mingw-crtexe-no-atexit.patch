# Purpose: Suppress atexit() definition in crtexe.c unconditionally — provided via UCRT import lib (api-ms-win-crt-runtime-l1-1-0) on all Windows arches
# Affects: lib/libc/mingw/crt/crtexe.c
# Upstream: not filed
# Drop-when: mingw-w64 ships atexit that does not conflict with UCRT alias on all arches
# Last-verified: 2026-05-18 (build 23)

--- a/lib/libc/mingw/crt/crtexe.c
+++ b/lib/libc/mingw/crt/crtexe.c
@@ -324,6 +324,7 @@
 	*av = n;
 }

+#if 0 /* suppressed: atexit provided via UCRT import lib (api-ms-win-crt-runtime-l1-1-0) on all Windows arches */
 int __cdecl atexit (_PVFV func)
 {
     /*
@@ -333,5 +334,6 @@
      */
     return _crt_atexit(func);
 }
+#endif /* 0 */

 char __mingw_module_is_dll = 0;
