--- a/lib/std/os/linux.zig
+++ b/lib/std/os/linux.zig
@@ -9802,8 +9802,14 @@
     };

     pub fn copy_file_range(fd_in: fd_t, off_in: ?*i64, fd_out: fd_t, off_out: ?*i64, len: usize, flags: u32) CopyFileRangeError!usize {
-        const use_c = std.c.versionCheck(if (builtin.abi.isAndroid()) .{ .major = 34, .minor = 0, .patch = 0 } else .{ .major = 2, .minor = 27, .patch = 0 });
-        const sys = if (use_c) std.c else std.os.linux;
+        // Use a comptime if so the not-taken branch is never type-checked or
+        // emitted by the compiler.  When targeting glibc < 2.27 (e.g. 2.17),
+        // use_c evaluates to false at comptime and std.c.copy_file_range is
+        // never referenced, avoiding a link failure against old sysroots.
+        // The Android API-34 guard is preserved through versionCheck's own
+        // android branch (see std/c.zig:versionCheck).
+        const use_c = comptime std.c.versionCheck(if (builtin.abi.isAndroid()) .{ .major = 34, .minor = 0, .patch = 0 } else .{ .major = 2, .minor = 27, .patch = 0 });
+        const sys = if (comptime use_c) std.c else std.os.linux;
         const rc = sys.copy_file_range(fd_in, off_in, fd_out, off_out, len, flags);
         switch (errno(rc)) {
             .SUCCESS => return @intCast(rc),
