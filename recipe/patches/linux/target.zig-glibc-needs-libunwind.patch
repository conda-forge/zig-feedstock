# Purpose: Force libunwind dependency for all glibc targets (not just static), required by conda-forge glibc 2.17 floor
# Affects: src/target.zig
# Upstream: not filed
# Drop-when: conda-forge linux glibc floor reaches a version that no longer needs libunwind forced
# Last-verified: 2026-05-13 (build 23, zig 0.16.0)

--- a/src/target.zig
+++ b/src/target.zig
@@ -32,7 +32,8 @@
 }

 pub fn libCNeedsLibUnwind(target: *const std.Target, link_mode: std.lang.LinkMode) bool {
-    return target.isGnuLibC() and link_mode == .static;
+    _ = link_mode;
+    return target.isGnuLibC();
 }

 pub fn libCxxNeedsLibUnwind(target: *const std.Target) bool {
