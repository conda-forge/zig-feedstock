# Purpose: Default allow_so_scripts to true so ld-script .so files work without -fallow-so-scripts
# Affects: src/link.zig
# Upstream: TBD
# Drop-when: Upstream sets allow_so_scripts = true by default or removes the flag
# Last-verified: 2026-05-12

--- a/src/link.zig
+++ b/src/link.zig
@@ -1608,7 +1608,7 @@
         reexport: bool = false,
         must_link: bool = false,
         hidden: bool = false,
-        allow_so_scripts: bool = false,
+        allow_so_scripts: bool = true,
         preferred_mode: std.builtin.LinkMode,
         search_strategy: SearchStrategy,

