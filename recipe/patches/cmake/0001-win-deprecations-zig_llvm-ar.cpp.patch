# Purpose: Replace close() with _close() in zig_llvm-ar.cpp to eliminate MSVC deprecation warnings
# Affects: src/zig_llvm-ar.cpp
# Upstream: TBD
# Drop-when: Upstream zig_llvm-ar.cpp uses _close() on MSVC builds, or MSVC removes the deprecation
# Last-verified: 2026-05-12

--- a/src/zig_llvm-ar.cpp
+++ b/src/zig_llvm-ar.cpp
@@ -620,8 +620,8 @@
         sys::fs::setLastAccessAndModificationTime(FD, ModTimeOrErr.get()));
   }

-  if (close(FD))
-    fail("Could not close the file");
+  if (_close(FD))
+    fail("Could not _close the file");
 }

 static bool shouldCreateArchive(ArchiveOperation Op) {
