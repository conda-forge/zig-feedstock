# Variant configuration for rattler-build
# This replaces conda_build_config.yaml

# C standard library version requirements
# For Linux
c_stdlib_version:
  - if: linux
    then: 2.28
# For macOS x86_64
# Note: osx-arm64 uses 11.0 which is already set in CI configs
